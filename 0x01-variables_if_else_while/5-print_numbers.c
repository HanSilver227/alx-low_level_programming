#include <stdio.h>
/**
 *main - Entry block
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int numbers;

	for (numbers = 1; numbers < 10; numbers++)
		printf("%d\n", numbers);
	return (0);
}
